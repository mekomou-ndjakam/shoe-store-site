# OS files
.DS_Store
Thumbs.db
Desktop.ini

# Editors
.vscode/
.idea/
*.iml

# Local-only files
*.log
*.local

# Flutter/Dart build artifacts (regenerated by `flutter pub get` / `flutter build`)
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/

# Bulk product photo library: ~10k files / 2.7GB, not meant to live in git.
# The app only bundles a curated subset via pubspec.yaml's explicit asset
# list (regenerate with `dart run tool/generate_asset_index.dart`).
assets/products/store_images/

# Local Netlify folder
.netlify
